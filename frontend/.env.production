# Production environment variables
# Leave VITE_API_BASE empty to use relative URLs (same-origin requests)
# This is correct for Heroku deployment where frontend and backend are on the same domain
VITE_API_BASE=